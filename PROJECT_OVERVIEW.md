# 项目总览

## 研究问题

现有MLLM可能依赖对象词共现或整体语义捷径，却不能稳定区分只改变主客体、属性绑定、空间关系或谓词关系的文本。普通caption数据很少系统控制“句法高度相似但关键事实冲突”这一变量，因此既难训练，也难准确测试。

## 核心假设

依存三元组 `<child, relation, head>` 能把关键关系从自然语言表面形式中显式分离。围绕三元组构造最小语义扰动，并把正确答案和关键关系共同作为监督，可以增强模型对细粒度结构差异的判断与解释能力。

## 两项核心贡献

### 1. 句法高度相似困难负文本与测试集

先解析正确caption，识别可扰动关系，再要求文本生成器只改变一个关键事实并尽量保留原句结构。之后使用文本合法性、依存树编辑距离、依存边Jaccard、语义相似度与NLI进行验证。最终样本同时保留正负文本、扰动类型、目标三元组和统计指标，可用于训练或held-out测试。

### 2. 句法三元组微调框架

训练分为三个连续阶段：

- CoT SFT：学习固定的“关键关系、视觉核验、对比、答案”输出结构。
- Syntax-first DPO：以正确解释为chosen，以错误答案、错误关系和错误视觉解释为rejected。
- Syntax-first RL：同一输入采样多个响应，按答案、方向敏感三元组、联合正确和格式计算组内相对优势。

## Image Route定位

Image Route对Qwen-VL原始视觉token生成权重，可用于估计teacher triple与视觉token路径的一致性。它对“优先扰动哪个三元组”有研究价值，但当前属于自研且效果仍需独立验证。因此：

- 主方法以文本解析三元组、句法相似度和NLI为依据。
- Image Route只作为可选排序信号和消融变量。
- RL中的Route权重默认关闭，避免错误视觉路由放大训练偏差。

## 当前范围

当前实现主要在约1万条MSCOCO样本上验证；E2 DPO只完成约1万偏好对训练；RL完成5%实验；held-out测试主要报告100条子集。扩大数据来源、训练epoch、DPO覆盖、测试规模和Image Route质量属于后续改进，不改变当前方法框架。
